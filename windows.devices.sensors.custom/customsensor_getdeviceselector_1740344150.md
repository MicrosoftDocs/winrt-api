---
-api-id: M:Windows.Devices.Sensors.Custom.CustomSensor.GetDeviceSelector(System.Guid)
-api-type: winrt method
---

<!-- Method syntax
public string GetDeviceSelector(System.Guid interfaceId)
-->

# Windows.Devices.Sensors.Custom.CustomSensor.GetDeviceSelector

## -description
Gets the device selector from the given interface identifier.

## -parameters
### -param interfaceId
The interface [Guid](/dotnet/api/system.guid?view=dotnet-uwp-10.0&preserve-view=true) that is associated with the custom sensor. This [Guid](/dotnet/api/system.guid?view=dotnet-uwp-10.0&preserve-view=true) is defined and registered by the sensor.

## -returns
Returns the device selector, if it exists; otherwise, null.

## -remarks

## -examples

## -see-also

---
-api-id: T:Windows.UI.StartScreen.TileMixedRealityModelActivationBehavior
-api-type: winrt enum
---

<!-- Enumeration syntax.
public enum TileMixedRealityModelActivationBehavior : int 
-->

# Windows.UI.StartScreen.TileMixedRealityModelActivationBehavior

## -description
Specifies how the system reacts when the user interacts with a [TileMixedRealityModel](tilemixedrealitymodel.md) in the Windows Mixed Reality home environment.

## -enum-fields
### -field None:1
When the user selects the tile model, the system does not launch the app.

### -field Default:0
When the user selects the tile model, the system launches the app.

## -remarks

## -see-also
[TileMixedRealityModel class](tilemixedrealitymodel.md)

## -examples


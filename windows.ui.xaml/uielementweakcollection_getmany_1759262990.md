---
-api-id: M:Windows.UI.Xaml.UIElementWeakCollection.GetMany(System.UInt32,Windows.UI.Xaml.UIElement[])
-api-type: winrt method
ms.custom: 19H1
---

<!-- Method syntax.
public uint UIElementWeakCollection.GetMany(UInt32 startIndex, UIElement[] items)
-->

# Windows.UI.Xaml.UIElementWeakCollection.GetMany

## -description

Retrieves multiple elements in a single pass through the iterator.

Equivalent **WinUI 2 API for UWP**: [Microsoft.UI.Xaml.UIElementWeakCollection.GetMany](/windows/winui/api/microsoft.ui.xaml.uielementweakcollection.getmany) (for WinUI in the [Windows App SDK](/windows/apps/windows-app-sdk/), see the **[Windows App SDK namespaces](/windows/windows-app-sdk/api/winrt/)**).

## -parameters
### -param startIndex

The index from which to start retrieval.

### -param items

Provides the destination for the result. Size the initial array size as a "capacity" in order to specify how many results should be retrieved.

## -returns

The number of items retrieved.

## -remarks

## -see-also

## -examples

